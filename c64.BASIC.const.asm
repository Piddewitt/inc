; --------------------------------------------------------------------------------------------------------------------- ;
; C64 BASIC V2 - Constants
; --------------------------------------------------------------------------------------------------------------------- ;
PI                            = $ff               ; 
NUMLEV                        = $17               ; 
; --------------------------------------------------------------------------------------------------------------------- ;
CLMWID                        = $0a               ; print window 10 chars
FORSIZ                        = [[ADDPRC * $02] + $10] ; $12 - numer of bytes for each FOR-NEXT loop
; --------------------------------------------------------------------------------------------------------------------- ;
NCMPOS                        = $1e               ; 
; --------------------------------------------------------------------------------------------------------------------- ;
ADDPRC                        = $01               ; 
ADDPR2                        = [ADDPRC + ADDPRC] ; 
ADDPR4                        = [ADDPR2 + ADDPR2] ; 
ADDPR8                        = [ADDPR4 + ADDPR4] ; 
; --------------------------------------------------------------------------------------------------------------------- ;
